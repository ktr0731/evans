box: golang

build:
  steps:
    # Sets the go workspace and places you package
    # at the right place in the workspace tree
    - setup-go-workspace

    - script:
        name: deps
        code: |
          make deps

    - script:
        name: build
        code: |
          make build

    - script:
        name: test
        code: |
          make test
